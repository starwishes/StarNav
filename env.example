# 后端认证配置
# JWT 签名密钥，建议使用随机长字符串
JWT_SECRET=your_jwt_secret_here

# 管理员登录凭据
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password_here

# 服务器配置
PORT=3000

# 说明：
# 1. JWT_SECRET - 用于加密和验证令牌，必须保持私密。
# 2. ADMIN_USERNAME - 后台管理系统的用户名（默认为 admin）。
# 3. ADMIN_PASSWORD - 后台管理系统的密码（默认为 admin123）。
#    注意：首次启动后，系统会自动对明文密码进行哈希处理。
# 4. PORT - 服务监听端口（默认为 3000）。

